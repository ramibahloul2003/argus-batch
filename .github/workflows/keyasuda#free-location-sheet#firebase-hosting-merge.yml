# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    env:
      API_KEY: ${{ secrets.API_KEY }}
      CLIENT_ID: ${{ secrets.CLIENT_ID }}
      RAKUTEN_APPLICATION_ID: ${{ secrets.RAKUTEN_APPLICATION_ID }}
      RAKUTEN_ACCESS_KEY: ${{ secrets.RAKUTEN_ACCESS_KEY }}

    runs-on: ubuntu-latest
    permissions:
      deployments: write
      pull-requests: write
      checks: write

    steps:
      - uses: actions/checkout@v3

      - name: Create GitHub deployment
        uses: chrnorm/deployment-action@v2.0.5
        id: deployment
        with:
          token: '${{ secrets.GITHUB_TOKEN }}'
          environment-url: https://wms.suzuran.dev/
          environment: production

      - name: Setup Ruby, JRuby and TruffleRuby
        uses: ruby/setup-ruby@v1.206.0
      - name: Use Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: build
        run: |
          ruby settings_writer.rb
          npm i --legacy-peer-deps && npm run build:production

      - name: deploy
        uses: FirebaseExtended/action-hosting-deploy@v0.7.1
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_QUICKSTART_1614336716847 }}'
          channelId: live
          target: quickstart-1614336716847
          projectId: quickstart-1614336716847

      - name: Update deployment status (success)
        if: success()
        uses: chrnorm/deployment-status@v2.0.1
        with:
          token: '${{ secrets.GITHUB_TOKEN }}'
          environment-url: https://wms.suzuran.dev/
          environment: production
          state: 'success'
          deployment-id: ${{ steps.deployment.outputs.deployment_id }}
