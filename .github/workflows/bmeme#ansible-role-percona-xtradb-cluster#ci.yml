name: Ansible Role Percona XtraDB Cluster
'on':
  push:
    branches-ignore:
      - main
  workflow_dispatch:

jobs:

  lint:
    name: Linting ansible role bmeme.percona_xtradb_cluster
    runs-on: ubuntu-latest
    steps:
      - 
        name: Check out the codebase.
        uses: actions/checkout@v3
      -
        name: Run ansible-lint
        uses: ansible/ansible-lint@main
  
  # test:
  #   name: Testing bmeme.percona_xtradb_cluster role 8.x and 9.x Ansible version
  #   runs-on: ubuntu-24.04
  #   needs: [lint]
  #   strategy:
  #     matrix:
  #       ansible:
  #         - "2.17"
  #       distro:
  #         - "rockylinux:9.4-2.15"
  #   steps:
  #     - 
  #       name: Check out the codebase.
  #       uses: actions/checkout@v3
  #     -
  #       name: Set up Python 3.
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: '3.10'
  #     -
  #       name: Install requirements
  #       run: pip install ansible-core==${{ matrix.ansible }} molecule molecule-plugins docker
  #     -
  #       name: Run Molecule test
  #       run: molecule test -d docker --all
  #       env:
  #         ANSIBLE_FORCE_COLOR: '1'
  #         MOLECULE_DISTRO: ${{ matrix.distro }}
